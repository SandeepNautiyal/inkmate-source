package com.inkmate.app.process;

public class ProblemExamplesProcessor {
    public String createTreeFromArray(int [] arr){
        int length = arr.length;
        int levels = (int)(Math.log(length) /
                Math.log(2));
        for(int i = 0; i < arr.length; i++){

        }
        return null;
    }
}
